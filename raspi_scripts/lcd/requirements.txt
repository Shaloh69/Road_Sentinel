# ─────────────────────────────────────────────────────────────────────────────
# Raspberry Pi 5 — HUB75 LED matrix dependencies
#
# PioMatter IS pip-installable on Pi 5 (unlike the old hzeller library).
# Run the full setup in lcd/README.md first (udev rules, venv, etc.)
# ─────────────────────────────────────────────────────────────────────────────

# HUB75 LED matrix driver for Raspberry Pi 5 (uses RP1 PIO blocks)
Adafruit-Blinka-Raspberry-Pi5-Piomatter>=0.4.0
adafruit-blinka

# Framebuffer array operations
numpy>=1.26.0

# Image rendering (text, color, shapes)
Pillow>=9.0.0

# HTTP client for Node Service API polling
requests>=2.31.0
