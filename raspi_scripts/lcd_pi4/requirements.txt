# ─────────────────────────────────────────────────────────────────────────────
# Raspberry Pi 4 Model B — HUB75 LED matrix dependencies
#
# rgbmatrix is NOT pip-installable — it must be built from C source.
# Run install.sh (or follow lcd_pi4/README.md Step 3) to build it.
# ─────────────────────────────────────────────────────────────────────────────

# Image rendering (text, color, shapes)
Pillow>=9.0.0

# HTTP client for Node Service API polling
requests>=2.31.0

# rgbmatrix — built from source, NOT via pip
# Build with: install.sh  (see README.md)
